# `pipetting_system` 인수인계 문서

## 0. 문서 목적

이 문서는 `/pipette_robot/pipetting_system` 아래 프로젝트를 다음 담당자가 빠르게 이해하고 실행/운용할 수 있도록 정리한 인수인계 문서입니다.  
정리 범위는 다음과 같습니다.

- 리팩토링 자료(구조, 전체 함수/클래스 목록, 파일별 상세 설명)
- 실행 및 운용 방법(개발 환경, 설정값, 실제 실행 절차)
- 인수인계를 위한 사용 방법 및 유의사항

본 문서는 **현재 저장소에 남아있는 코드 기준**으로 작성했습니다.

---

## 1. 프로젝트 개요

### 1.1 목적

이 프로젝트는 **UR 로봇 + AprilTag 비전 + Well Plate(4x6) 좌표 변환**을 결합해, 운영자가 GUI에서 버튼을 누르면 다음 기능을 수행하도록 구성된 통합 제어 시스템입니다.

- Alvium 카메라 프레임 수집(실시간)
- AprilTag(`tag36h11`) 검출 및 포즈 추정(pupil-apriltags)
- 태그 중심으로 “카메라/툴 장착각(기본 -60°)” 보정이 포함된 XY 상대 이동
- 4x6 Well Plate 각 웰(A1~D6)로 이동(태그 기준 로컬 좌표 → 카메라 좌표 → 로봇 상대 이동)
- 피펫팅 시퀀스(오프셋 이동 → Z 분할 하강 → Z 분할 상승 → 원위치)
- ROS 2 기반 UR 드라이버/MoveIt2 IK 연동 및 Trajectory 전송

### 1.2 전체 실행 구조(프로세스/모듈)

구조를 한 줄로 요약하면:

> **`main.py`(Tkinter GUI) ↔ `URInterface`(ROS2/MoveIt IK/Trajectory) + `TagDetectorWrapper`(카메라/AprilTag) + `AutomationController`(시퀀스/보정 로직)**

주요 흐름:

1. `main.py`가 Tkinter GUI(`CalibrationApp`)를 띄웁니다.
2. 같은 프로세스에서 ROS2 노드(`URInterface`)를 만들고, `SingleThreadedExecutor`를 별도 스레드로 spin 합니다.
3. 카메라(`AlviumCamera`)를 별도 스레드로 스트리밍하며, `TagDetectorWrapper`가 프레임을 가져와 AprilTag를 추정합니다.
4. GUI 버튼 동작은 `AutomationController`로 위임됩니다.
   - 태그 중앙 정렬(최대 N회 피드백)
   - Well 이동(태그 포즈를 기준으로 목표 웰 위치를 계산 후 상대 이동)
   - 피펫 시퀀스(Z 분할 이동 포함)

### 1.3 왜 이렇게 구성했는가(설계 의도)

- **ROS2/MoveIt과 GUI를 한 프로세스에서**: 운영자 피드백(영상/상태)과 로봇 이동 명령을 즉시 연결.
- **카메라/태그 처리와 로봇 제어를 느슨하게 결합**: `TagDetectorWrapper.latest_result`를 통해 최신 프레임/태그 포즈만 공유.
- **툴 장착각(-60°) 보정**: 카메라 좌표계에서 계산한 오차를 로봇/툴 좌표로 변환하는데 보정이 필요.
- **Z 분할 이동(충돌/정밀도)**: 하강을 다점 궤적으로 만들어 급격한 이동/충돌 위험을 줄임.

---

## 2. 디렉터리 구조

```text
pipette_robot/pipetting_system/
├── 설명서.md
├── main.py
├── automation_controller.py
├── robot_interface.py
├── vision_module.py
├── alvium_driver.py
└── config.py
```

### 2.1 각 파일 역할

- `main.py`: **GUI 진입점**. Tkinter UI 구성, 영상 표시, 버튼 이벤트 → 컨트롤러 호출.
- `automation_controller.py`: **자동화/시퀀스 로직 핵심**. 60도 보정 상대 이동, 태그 센터링, Well 이동, 피펫 시퀀스.
- `robot_interface.py`: **ROS2 로봇 인터페이스**. `/joint_states` 구독, TF 조회, MoveIt `/compute_ik` 서비스, FollowJointTrajectory 액션 전송.
- `vision_module.py`: **비전 모듈**. AprilTag 검출, 포즈 추정, 화면 오버레이/축 표시, 회전 오프셋 적용.
- `alvium_driver.py`: **Alvium 카메라 드라이버(vmbpy)**. 카메라 스트리밍 스레드 및 최신 프레임 제공.
- `config.py`: **상수/파라미터 집합**. 로봇 링크/그룹, 태그/플레이트 치수, 제어 파라미터(분할 이동, 센터링 임계 등), 피펫 오프셋.

---

## 3. 리팩토링 자료

## 3.1 개발한 전체 함수 / 클래스 목록(현재 코드 기준)

### 3.1.1 GUI 계층

#### `main.py`

- 클래스
  - `CalibrationApp`
- 메서드(주요)
  - `CalibrationApp.__init__`
  - `CalibrationApp._setup_ui`
  - `CalibrationApp._ui_tick`
  - `CalibrationApp._jog_joint`
  - `CalibrationApp._jog_pose`
  - `CalibrationApp._track_tag_once`
  - `CalibrationApp._go_home_sequence`
  - `CalibrationApp._run_pipette_sequence`
  - `CalibrationApp._on_close`
- 함수
  - `main`

### 3.1.2 자동화/제어 계층

#### `automation_controller.py`

- 클래스
  - `AutomationController`
- 메서드(주요)
  - `AutomationController.execute_relative_move`
  - `AutomationController.wait_for_tag_pose`
  - `AutomationController.center_tag_and_get_latest_pose`
  - `AutomationController.move_to_well`
  - `AutomationController.move_z_linear`
  - `AutomationController.run_pipette_sequence`

### 3.1.3 로봇 인터페이스(ROS2/MoveIt) 계층

#### `robot_interface.py`

- 데이터 클래스
  - `JointOrder`
- 클래스
  - `URInterface`
- 메서드(주요)
  - `URInterface._on_joint_states`
  - `URInterface.get_ik_sync`
  - `URInterface.solve_ik_and_move`
  - `URInterface._on_ik_done`
  - `URInterface.send_traj`
  - `URInterface.send_multi_point_traj`

### 3.1.4 비전 계층

#### `vision_module.py`

- 함수
  - `draw_axes`
  - `rmat_to_euler_zyx`
  - `draw_text_with_bg`
- 클래스
  - `TagDetectorWrapper`
- 메서드(주요)
  - `TagDetectorWrapper.apply_tag_rotation_offset`
  - `TagDetectorWrapper.process_latest_frame`

### 3.1.5 카메라 계층

#### `alvium_driver.py`

- 클래스
  - `AlviumCamera`
- 메서드(주요)
  - `AlviumCamera.start`
  - `AlviumCamera.stop`
  - `AlviumCamera.get_frame`
  - `AlviumCamera.get_intrinsics`
  - `AlviumCamera._setup_camera`
  - `AlviumCamera._run_camera_loop`

### 3.1.6 설정 계층

#### `config.py`

- 클래스(상수 컨테이너)
  - `RobotConfig`
  - `WellPlateConfig`
  - `ControlParams`
  - `PipetteConfig`

---

## 3.2 각 코드 및 함수별 상세 설명

### 3.2.1 가장 중요한 실행 흐름(인수인계 추천 순서)

1. `main.py` (`CalibrationApp`): GUI 이벤트가 어디로 연결되는지 확인
2. `automation_controller.py` (`AutomationController`): 이동/시퀀스의 “의도”가 구현된 곳
3. `robot_interface.py` (`URInterface`): IK/Trajectory 전송 및 ROS2 의존 포인트 확인
4. `vision_module.py` (`TagDetectorWrapper`): 태그 포즈가 어떤 좌표/보정으로 만들어지는지 확인
5. `config.py`: 모든 숫자(오프셋/간격/임계)가 어디서 오며 무엇을 의미하는지 확인
6. `run.sh`: 실제 현장 실행 프로세스 확인

### 3.2.2 `AutomationController.execute_relative_move` (60도 보정 상대 이동)

입력 `dx_cam, dy_cam`은 **카메라 좌표계에서의 오차(m)**로 취급됩니다.  
이를 다음 순서로 변환합니다.

- 툴 장착각 `RobotConfig.MOUNT_ANGLE_DEG`(기본 -60°)를 적용해 **카메라 오차 → 툴 좌표계 이동량**으로 변환
- 현재 TF의 툴 회전 `R_base_tool`을 적용해 **툴 좌표계 → base_link 좌표계 상대 이동량**으로 변환
- 최종 목표 위치(`target_pos`)를 만들고 `URInterface.solve_ik_and_move()`로 IK/이동을 수행

주의:

- TF가 준비되기 전에는 `can_transform(..., timeout=2s)`로 대기 후 실패 시 반환합니다.

### 3.2.3 `AutomationController.center_tag_and_get_latest_pose` (태그 센터링)

- `TagDetectorWrapper.latest_result`에서 태그의 픽셀 중심(`center_px`)을 읽습니다.
- 이미지 중심과의 오차를 $u\_err, v\_err$로 계산합니다.
- 카메라 내참 `K`를 이용해 픽셀 오차를 카메라 좌표의 거리 오차로 근사합니다.
- `execute_relative_move()`로 오차를 줄이는 방향으로 반복 이동합니다.

운용 파라미터:

- 임계 픽셀: `ControlParams.CENTER_THRESH_PX`
- 최대 반복: `ControlParams.CENTER_MAX_ITERS`
- 반복당 타임아웃: `ControlParams.CENTER_PER_ITER_TIMEOUT`

### 3.2.4 `AutomationController.move_to_well` (Well 이동)

전제:

- **태그가 플레이트 기준 프레임**처럼 사용됩니다.
- `WellPlateConfig.OFFSET_TAG_TO_REF_Y_MM`와 `WellPlateConfig.WELL_SPACING_MM`로 로컬 좌표를 정의합니다.

계산 흐름:

1. 목표 웰(row, col)을 로컬 좌표 `P_local`(m)로 만든다.
2. 태그 포즈(`R_mat`, `t_vec`)를 이용해 카메라 기준 웰 위치를 구한다: `P_well_in_cam = R_mat @ P_local + t_vec`
3. `move_x, move_y`를 만들어 `execute_relative_move()`로 이동한다.
4. 이동 시간은 거리 기반으로 `ControlParams.WELL_MOVE_*` 범위에서 클램프된다.

### 3.2.5 `AutomationController.move_z_linear` (Z 분할/다점 궤적)

- 목표 Z 이동량 `dz_total`을 `ControlParams.LINEAR_STEP_M` 간격으로 나누고,
- 각 점에 대해 `URInterface.get_ik_sync()`로 IK를 미리 풀어 `q_sequence`를 만든 뒤,
- `URInterface.send_multi_point_traj()`로 하나의 다점 궤적을 보냅니다.

주의:

- IK 실패 시 즉시 False로 반환합니다(현장에선 안전상 중요).
- `get_ik_sync()`는 서비스 준비 여부/스레딩에 민감하므로, **GUI 스레드에서 블로킹 호출을 직접 늘리지 않는 방향**이 안전합니다.

### 3.2.6 `TagDetectorWrapper.process_latest_frame` (태그 추정)

- 카메라 프레임을 가져온 뒤 grayscale로 변환하여 AprilTag 검출
- `estimate_tag_pose=True`로 포즈(`pose_R`, `pose_t`)를 얻음
- 회전 오프셋(`WellPlateConfig.TAG_*_OFFSET_DEG`)을 적용한 `R_mat` 생성
- 디버그를 위해 화면에 축/텍스트 오버레이를 그린 `display_frame` 제공
- 결과는 `latest_result`에 저장되어 다른 계층이 읽는다

---

## 4. 실행 및 운용 방법

## 4.1 필요한 개발 환경

### 4.1.1 운영체제(권장)

**Ubuntu + ROS 2 Jazzy** 환경을 가정합니다.

- 현장 운영은 보통 “터미널 2개(UR Driver/MoveIt) + 앱 1개” 형태로 구동합니다.

### 4.1.2 ROS 2 / MoveIt / UR 드라이버

필수 구성요소(동작에 반드시 필요):

- UR 드라이버: `ur_robot_driver` (예: `ur_control.launch.py`)
- MoveIt2 구성: `ur_moveit_config` (예: `ur_moveit.launch.py`)
- IK 서비스: `/compute_ik` (`moveit_msgs/srv/GetPositionIK`)
- Trajectory Action: `RobotConfig.ACTION_NAME`
- TF: `base_link` ↔ `tool0` 변환 제공

런치 기본값(현장 환경에 맞게 수정 필요):

- 로봇 IP: `192.168.64.106`
- UR 타입: `ur5e`

### 4.1.3 Python 환경 및 패키지

권장: 프로젝트 루트에 `.venv` 가상환경을 만들고 앱 실행 터미널에서 활성화합니다.

주요 파이썬 의존성:

- `rclpy` (ROS2 Python)
- `numpy`
- `scipy`
- `opencv-python`
- `pillow`
- `pupil-apriltags`
- (선택) `vmbpy` + Alvium SDK (Alvium 카메라 사용 시)

예시(최소):

```bash
pip install numpy scipy opencv-python pillow pupil-apriltags
```

### 4.1.4 주요 설정값(튜닝 포인트)

대부분의 숫자는 `config.py`에 있습니다.

- 로봇/프레임:
  - `RobotConfig.BASE_LINK` (`base_link`)
  - `RobotConfig.EE_LINK` (`tool0`)
  - `RobotConfig.MOUNT_ANGLE_DEG` (기본 -60°)
- 플레이트/태그:
  - `WellPlateConfig.TAG_SIZE_M`
  - `WellPlateConfig.WELL_SPACING_MM`
  - `WellPlateConfig.OFFSET_TAG_TO_REF_Y_MM`
  - `WellPlateConfig.TAG_*_OFFSET_DEG`
- 제어:
  - `ControlParams.CENTER_*`
  - `ControlParams.LINEAR_STEP_M`, `ControlParams.STEP_DURATION_SEC`
  - `ControlParams.WELL_MOVE_*`
- 피펫:
  - `PipetteConfig.PIPETTE_OFFSET_X/Y`, `PipetteConfig.PIPETTE_DOWN_Z`

---

## 4.2 실제 실행 절차

### 4.2.1 권장 실행(터미널 2개 + 앱 1개)

아래처럼 **터미널을 분리**해서 실행합니다.

#### 터미널 1) UR Driver 실행

```bash
source /opt/ros/jazzy/setup.bash
ros2 launch ur_robot_driver ur_control.launch.py ur_type:=ur5e robot_ip:=192.168.64.106 launch_rviz:=false
```

#### 터미널 2) MoveIt 실행

```bash
source /opt/ros/jazzy/setup.bash
ros2 launch ur_moveit_config ur_moveit.launch.py ur_type:=ur5e launch_rviz:=true publish_robot_description_semantic:=true
```

#### 터미널 3) GUI 앱 실행

```bash
source /opt/ros/jazzy/setup.bash
cd pipette_robot/pipetting_system
python3 main.py
```

운용 환경에 따라 수정이 필요한 값:

- `robot_ip` (UR 컨트롤러 IP)
- `ur_type` (예: `ur5e`)

### 4.2.2 수동 실행(디버깅 시)

1) ROS2 환경 로드:

```bash
source /opt/ros/jazzy/setup.bash
```

2) UR Driver / MoveIt 실행(별도 터미널 권장)

3) 파이썬 앱 실행:

```bash
cd pipette_robot/pipetting_system
python3 main.py
```

---

## 4.3 운용 시 사용 방법(현장 체크리스트)

권장 운용 순서:

1. UR Driver/MoveIt/TF 정상 여부 확인(로봇 상태, TF tree, `/compute_ik` 준비)
2. `main.py` 실행 후 GUI에서 **카메라 영상 표시** 확인
3. 태그가 화면에 들어오면 **Tag 탐지 상태/좌표가 갱신되는지** 확인
4. `태그 중앙 정렬` 버튼으로 센터링(수 회 반복 이동)
5. Well 버튼(A1~D6)으로 목표 위치 이동
6. 필요 시 피펫 시퀀스(오프셋/하강/복귀) 실행

문제 발생 시 1차 확인:

- 태그가 탐지되는데도 로봇이 반대로 움직인다 → `RobotConfig.MOUNT_ANGLE_DEG`, 축 스왑(`execute_relative_move`) 확인
- 센터링이 수렴하지 않는다 → `ControlParams.CENTER_THRESH_PX`, 카메라 내참(`get_intrinsics`) 및 태그 크기(`TAG_SIZE_M`) 확인
- IK 실패가 잦다 → 로봇 초기 자세, `HOME_JOINTS`, MoveIt 파라미터/충돌, 목표 포즈 범위 확인
- Z 분할 이동 중 멈춘다 → `get_ik_sync`에서 특정 스텝 IK 실패, step 크기(`LINEAR_STEP_M`) 또는 목표 Z 확인

---

## 5. 인수인계를 위한 유의사항

### 5.1 가장 중요한 운영 전제

- **ROS2/MoveIt 의존**이 강합니다. `/compute_ik`, FollowJointTrajectory 액션, TF가 준비되지 않으면 대부분 기능이 동작하지 않습니다.
- **카메라 드라이버 의존**이 있습니다. Alvium을 쓰지 않는 환경에서는 `alvium_driver.py` ImportError로 대체 클래스가 사용되며, 이 경우 “카메라가 실제로는 동작하지 않을 수 있음”을 인지해야 합니다.

### 5.2 코드 관점 주의 포인트

- `main.py`에서 ROS2 executor를 별도 스레드로 spin 합니다. 종료 시 `executor.shutdown()` 및 `rclpy.shutdown()` 순서/예외 처리가 중요합니다.
- `URInterface.get_ik_sync()`는 서비스 준비/스레딩에 민감합니다(블로킹 호출). 다점 궤적 생성에만 사용되고, 일반 이동은 비동기(`call_async`)를 씁니다.
- `TagDetectorWrapper.latest_result`는 “마지막 프레임 기준”입니다. 타임스탬프 기반 `min_timestamp` 가드가 센터링 품질에 중요합니다.

### 5.3 인수인계 추천 전달 순서(요약)

1. 터미널 2개(UR Driver/MoveIt) + 앱 1개로 전체 시스템 기동 방법 설명
2. `main.py`에서 GUI 버튼이 무엇을 호출하는지 설명
3. `automation_controller.py`에서 보정/시퀀스/Well 변환 로직 설명
4. `robot_interface.py`에서 ROS2/MoveIt 연동 포인트(토픽/액션/서비스/TF) 설명
5. `vision_module.py`에서 태그 추정/오프셋/내참 파라미터 설명
6. `config.py`의 상수들을 “현장 튜닝 포인트”로 정리해서 전달

---

## 6. 참고

- GUI 진입점: `pipette_robot/pipetting_system/main.py`
- 자동화 로직: `pipette_robot/pipetting_system/automation_controller.py`
- 로봇 인터페이스: `pipette_robot/pipetting_system/robot_interface.py`
- 비전 모듈: `pipette_robot/pipetting_system/vision_module.py`
- 설정값: `pipette_robot/pipetting_system/config.py`

---

## 7. 한 줄 요약

이 프로젝트는 **ROS2(UR Driver/MoveIt) + Alvium 카메라 + AprilTag 기반 위치 보정 + Well Plate/피펫 시퀀스를 Tkinter GUI로 묶은 통합 자동화 시스템**입니다.

